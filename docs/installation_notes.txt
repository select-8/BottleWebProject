

Create Virtual Environment using virtualenv (replacing the paths to whatever they might be on your machine):
    Open a terminal and cd to the BottleWebProject folder, then do:ß
        C:\Python38\Scripts\virtualenv.exe C:\VirtualEnvs\bottle-project
        C:\VirtualEnvs\bottle-project\Scripts\activate

Install from requirments.txt: 
    pip install -r requirments.txt

To run the app:
    python app.py